{
  "builds": [
    {
      "src": "apps/vite-web/package.json",
      "use": "@vercel/static-build",
      "config": { "distDir": "dist" }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "/apps/vite-web/dist/$1" }
  ]
}
